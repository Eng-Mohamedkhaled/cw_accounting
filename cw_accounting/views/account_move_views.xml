<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Inherit the account.move form view to add the duplicate entry field -->
    <record id="view_account_move_form_inherit_duplicate_entry" model="ir.ui.view">
        <field name="name">account.move.form.inherit.duplicate.entry</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="before">
                <group>

               
                <field name="duplicate_entry_id"
                       string="Auto Complete"
                       placeholder="Select a previous journal entry to copy from"
                       invisible="state != 'draft' or move_type != 'entry'"/>
                        </group>
            </xpath>
        </field>
    </record>
</odoo>